# M1 阶段计划任务：本地 Todo 核心功能

> 文档版本：v1.0
> 编制日期：2026-07-22
> 阶段目标：完成本地离线任务管理，断网时可以独立使用全部基础功能
> 前置依赖：M0 阶段完成

---

## 一、任务总览

| 任务组 | 任务数 | 优先级 | 预计产出 |
|---|---|---|---|
| 本地数据库初始化 | 9 | P0 | SQLite 数据库 |
| 核心数据表 | 11 | P0/P1 | 数据库表结构 |
| tasks 字段定义 | 18 | P0/P1 | 任务表字段 |
| 数据完整性 | 7 | P0 | 约束和索引 |
| 主窗口 | 7 | P0/P1 | 窗口管理 |
| 系统托盘 | 6 | P0/P1 | 托盘功能 |
| 快捷键 | 7 | P0/P1 | 键盘快捷键 |
| 自动更新 | 5 | P1 | 更新机制 |
| 基础 CRUD | 11 | P0 | 任务增删改查 |
| 内置视图 | 9 | P0/P1 | 预设视图 |
| 搜索筛选排序 | 7 | P0/P1 | 搜索功能 |
| 批量操作 | 7 | P0/P1 | 批量处理 |
| 父子任务实现 | 7 | P0 | 层级任务 |

**总计**：约 100 个任务项

---

## 二、本地数据库初始化

### 2.1 SQLite 配置（P0）

- [ ] **DB-001** 接入 Tauri SQL 插件和 SQLite
  - 安装 `@tauri-apps/plugin-sql`
  - 配置插件权限

- [ ] **DB-002** 建立版本化迁移机制
  - 创建 `migrations` 目录
  - 使用数字版本号（如 `001_init.sql`）
  - 记录当前版本到 `schema_version` 表

- [ ] **DB-003** 启用 `PRAGMA foreign_keys = ON`
  - 每次连接时执行
  - 验证外键约束生效

- [ ] **DB-004** 评估并启用 WAL 模式
  - `PRAGMA journal_mode=WAL`
  - 测试并发读写性能

- [ ] **DB-005** 确认 SQLite 版本已修复 WAL-reset 问题
  - 检查 Tauri 打包的 SQLite 版本
  - 建立并发写入测试

- [ ] **DB-006** 设置合理的 `busy_timeout`
  - `PRAGMA busy_timeout=5000`
  - 测试高并发场景

- [ ] **DB-007** 所有批量修改使用事务
  - 封装事务工具函数
  - 确保原子性

- [ ] **DB-008** 应用启动时自动执行迁移
  - 检查 `schema_version` 表
  - 按顺序执行待执行的迁移
  - 失败时停止写入并提示备份

- [ ] **DB-009** 数据库升级前自动复制备份
  - 备份文件名：`eztodo.db.backup.{timestamp}`
  - 保留最近 3 个备份

**产出物**：`apps/desktop/src/lib/database/`

---

## 三、核心数据表

### 3.1 任务相关表（P0）

- [ ] **TBL-001** `tasks` 表
  ```sql
  CREATE TABLE tasks (
    id TEXT PRIMARY KEY,           -- UUIDv7
    parent_id TEXT REFERENCES tasks(id),
    project_id TEXT REFERENCES projects(id),
    title TEXT NOT NULL,
    note TEXT,
    status TEXT DEFAULT 'todo',    -- todo/done/cancelled
    priority TEXT DEFAULT 'none',  -- p1/p2/p3/p4/none
    sort_order INTEGER DEFAULT 0,
    scheduled_date TEXT,           -- YYYY-MM-DD
    scheduled_at TEXT,             -- ISO 8601 UTC
    due_at TEXT,                   -- ISO 8601 UTC
    is_all_day INTEGER DEFAULT 0,
    timezone TEXT DEFAULT 'Asia/Shanghai',
    estimated_minutes INTEGER,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    completed_at TEXT,
    deleted_at TEXT,
    revision INTEGER DEFAULT 1,
    source TEXT DEFAULT 'manual',  -- manual/ai/import/api
    source_capture_id TEXT
  );
  ```

- [ ] **TBL-002** `projects` 表
  ```sql
  CREATE TABLE projects (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    color TEXT,
    icon TEXT,
    sort_order INTEGER DEFAULT 0,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    deleted_at TEXT
  );
  ```

- [ ] **TBL-003** `tags` 表
  ```sql
  CREATE TABLE tags (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    color TEXT,
    created_at TEXT NOT NULL
  );
  ```

- [ ] **TBL-004** `task_tags` 表
  ```sql
  CREATE TABLE task_tags (
    task_id TEXT REFERENCES tasks(id) ON DELETE CASCADE,
    tag_id TEXT REFERENCES tags(id) ON DELETE CASCADE,
    PRIMARY KEY (task_id, tag_id)
  );
  ```

- [ ] **TBL-005** `reminders` 表
  ```sql
  CREATE TABLE reminders (
    id TEXT PRIMARY KEY,
    task_id TEXT NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    remind_at TEXT NOT NULL,
    status TEXT DEFAULT 'pending',  -- pending/triggered/confirmed/snoozed/cancelled
    snoozed_until TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
  );
  ```

- [ ] **TBL-006** `recurrence_rules` 表
  ```sql
  CREATE TABLE recurrence_rules (
    id TEXT PRIMARY KEY,
    task_id TEXT NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    frequency TEXT NOT NULL,        -- daily/weekly/monthly/yearly/custom
    interval INTEGER DEFAULT 1,
    days_of_week TEXT,              -- JSON array
    day_of_month INTEGER,
    month_of_year INTEGER,
    start_date TEXT NOT NULL,
    end_date TEXT,
    max_occurrences INTEGER,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
  );
  ```

- [ ] **TBL-007** `task_events` 表
  ```sql
  CREATE TABLE task_events (
    id TEXT PRIMARY KEY,
    task_id TEXT NOT NULL,
    event_type TEXT NOT NULL,       -- created/updated/completed/deleted
    changes TEXT,                   -- JSON
    created_at TEXT NOT NULL
  );
  ```

### 3.2 同步相关表（P0）

- [ ] **TBL-008** `sync_outbox` 表
  ```sql
  CREATE TABLE sync_outbox (
    id TEXT PRIMARY KEY,
    operation_id TEXT NOT NULL UNIQUE,
    entity_type TEXT NOT NULL,      -- task/reminder/recurrence_rule/project/tag
    entity_id TEXT NOT NULL,
    operation TEXT NOT NULL,        -- create/update/delete
    payload TEXT NOT NULL,          -- JSON
    base_revision INTEGER,
    created_at TEXT NOT NULL,
    synced_at TEXT
  );
  ```

- [ ] **TBL-009** `sync_state` 表
  ```sql
  CREATE TABLE sync_state (
    id TEXT PRIMARY KEY,
    device_id TEXT NOT NULL,
    last_cursor TEXT,
    last_sync_at TEXT,
    server_version INTEGER DEFAULT 0,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
  );
  ```

### 3.3 配置相关表（P0/P1）

- [ ] **TBL-010** `settings` 表
  ```sql
  CREATE TABLE settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL,
    updated_at TEXT NOT NULL
  );
  ```

- [ ] **TBL-011** `ai_captures` 表（P0）
  ```sql
  CREATE TABLE ai_captures (
    id TEXT PRIMARY KEY,
    original_text TEXT NOT NULL,
    status TEXT DEFAULT 'pending',  -- pending/processing/completed/failed
    confidence TEXT,                -- high/medium/low
    parsed_tasks TEXT,              -- JSON
    warnings TEXT,                  -- JSON array
    error TEXT,
    created_at TEXT NOT NULL,
    completed_at TEXT
  );
  ```

- [ ] **TBL-012** `import_jobs` 表（P1）
  ```sql
  CREATE TABLE import_jobs (
    id TEXT PRIMARY KEY,
    file_name TEXT NOT NULL,
    file_type TEXT NOT NULL,        -- csv/json
    status TEXT DEFAULT 'pending',
    total_count INTEGER,
    success_count INTEGER,
    error_count INTEGER,
    errors TEXT,                    -- JSON
    created_at TEXT NOT NULL,
    completed_at TEXT
  );
  ```

**产出物**：`apps/desktop/src/lib/database/migrations/`

---

## 四、数据完整性

### 4.1 约束和索引（P0）

- [ ] **INT-001** 标题去除首尾空白，空标题不得保存
  - 在 Repository 层处理
  - 返回明确错误信息

- [ ] **INT-002** 设置标题、备注大小上限
  - 标题：500 字符
  - 备注：10000 字符

- [ ] **INT-003** 外键和唯一索引设计完成
  - 所有外键约束
  - `sync_outbox.operation_id` 唯一约束

- [ ] **INT-004** 为常用查询建立索引
  ```sql
  CREATE INDEX idx_tasks_status ON tasks(status) WHERE deleted_at IS NULL;
  CREATE INDEX idx_tasks_parent_id ON tasks(parent_id) WHERE deleted_at IS NULL;
  CREATE INDEX idx_tasks_project_id ON tasks(project_id) WHERE deleted_at IS NULL;
  CREATE INDEX idx_tasks_scheduled_date ON tasks(scheduled_date) WHERE deleted_at IS NULL;
  CREATE INDEX idx_tasks_due_at ON tasks(due_at) WHERE deleted_at IS NULL;
  CREATE INDEX idx_tasks_deleted_at ON tasks(deleted_at) WHERE deleted_at IS NOT NULL;
  CREATE INDEX idx_tasks_updated_at ON tasks(updated_at);
  CREATE INDEX idx_reminders_task_id ON reminders(task_id);
  CREATE INDEX idx_reminders_remind_at ON reminders(remind_at) WHERE status = 'pending';
  ```

- [ ] **INT-005** 禁止父子任务形成环
  - 移动前检查祖先链
  - 返回明确错误

- [ ] **INT-006** 软删除记录保留同步墓碑
  - `deleted_at` 字段
  - 未同步前不得物理删除

- [ ] **INT-007** 物理清理回收站前确认同步
  - 检查 `sync_outbox` 是否有未同步操作
  - 检查所有活动设备是否已收到删除事件

**产出物**：`apps/desktop/src/lib/database/constraints.ts`

---

## 五、Windows 客户端外壳

### 5.1 主窗口（P0）

- [ ] **WIN-001** 保存并恢复窗口大小、位置和最大化状态
  - 使用 `localStorage` 或 `settings` 表
  - 启动时恢复

- [ ] **WIN-002** 双屏环境下验证窗口不会恢复到不可见区域
  - 检查窗口位置是否在可见显示器范围内
  - 超出时重置到主屏幕中心

- [ ] **WIN-003** 支持浅色、深色和跟随系统主题
  - 主题设置保存到 `settings`
  - 使用 CSS 变量实现主题切换

- [ ] **WIN-004** 支持 100%、125%、150%、200% DPI 缩放
  - 测试各 DPI 下的显示效果
  - 确保文字和图标清晰

- [ ] **WIN-005** 防止启动多个应用实例
  - 使用 `@tauri-apps/plugin-single-instance`（如可用）
  - 或使用文件锁机制

- [ ] **WIN-006** 第二次启动时唤醒已有窗口
  - 发送 IPC 消息
  - 将已有窗口置于前台

- [ ] **WIN-007** 支持最小化启动（P1）
  - 命令行参数 `--minimized`
  - 启动后直接最小化到托盘

**产出物**：`apps/desktop/src/lib/window.ts`

---

### 5.2 系统托盘（P0）

- [ ] **TRAY-001** 创建托盘图标
  - 图标文件：`src-tauri/icons/tray.ico`
  - 支持 16x16 和 32x32

- [ ] **TRAY-002** 单击托盘图标显示或隐藏主窗口
  - 切换窗口可见性
  - 窗口显示时置于前台

- [ ] **TRAY-003** 托盘菜单包含"快速添加、今天、同步、设置、退出"
  - 快速添加：打开快速添加窗口
  - 今天：显示今天视图
  - 同步：触发同步
  - 设置：打开设置页
  - 退出：退出应用

- [ ] **TRAY-004** 点击窗口关闭按钮时默认隐藏到托盘
  - 拦截关闭事件
  - 最小化到托盘

- [ ] **TRAY-005** 真正退出前提示"退出后将无法收到本地提醒"
  - 确认对话框
  - 用户确认后才退出

- [ ] **TRAY-006** 支持开机自启动开关（P1）
  - 使用 Tauri API 或注册表
  - 设置保存到 `settings`

**产出物**：`apps/desktop/src/lib/tray.ts`

---

### 5.3 快捷键（P0/P1）

- [ ] **KEY-001** `Ctrl+N`：新建普通任务
  - 打开任务创建对话框
  - 焦点自动到标题输入框

- [ ] **KEY-002** `Ctrl+K`：全局搜索
  - 打开搜索框
  - 支持标题、备注搜索

- [ ] **KEY-003** `Ctrl+Enter`：保存任务
  - 在任务编辑面板中生效
  - 保存并关闭面板

- [ ] **KEY-004** `Esc`：关闭详情或弹窗
  - 关闭当前打开的面板或对话框

- [ ] **KEY-005** 注册全局快捷键 `Ctrl+Shift+A`（P1）
  - 应用不在前台时也能触发
  - 打开快速添加窗口

- [ ] **KEY-006** 全局快捷键打开轻量快速添加窗口（P1）
  - 小窗口，只包含标题输入
  - 支持 AI 创建

- [ ] **KEY-007** 快捷键冲突时允许用户重新设置（P1）
  - 设置页配置
  - 保存到 `settings`

**产出物**：`apps/desktop/src/lib/shortcuts.ts`

---

## 六、本地任务功能

### 6.1 基础 CRUD（P0）

- [ ] **CRUD-001** 新建任务
  - 必填：标题
  - 可选：项目、优先级、日期、备注
  - 自动生成 UUIDv7
  - 写入 `tasks` 表和 `sync_outbox`

- [ ] **CRUD-002** 编辑标题和备注
  - 实时保存（防抖 500ms）
  - 写入 `sync_outbox`

- [ ] **CRUD-003** 标记完成和取消完成
  - 切换 `status` 字段
  - 设置/清除 `completed_at`
  - 父任务联动检查

- [ ] **CRUD-004** 软删除任务
  - 设置 `deleted_at`
  - 子任务一起删除
  - 写入 `sync_outbox`

- [ ] **CRUD-005** 复制任务
  - 新 UUID
  - 复制标题、备注、优先级
  - 不复制完成状态、日期

- [ ] **CRUD-006** 移动到其他项目
  - 更新 `project_id`
  - 写入 `sync_outbox`

- [ ] **CRUD-007** 设置优先级
  - P1/P2/P3/P4/none
  - 写入 `sync_outbox`

- [ ] **CRUD-008** 添加和删除标签
  - 操作 `task_tags` 表
  - 写入 `sync_outbox`

- [ ] **CRUD-009** 拖动调整同级任务顺序
  - 更新 `sort_order`
  - 写入 `sync_outbox`

- [ ] **CRUD-010** 所有修改立即写入本地数据库
  - 不依赖服务器响应
  - 离线时正常工作

- [ ] **CRUD-011** 每次修改同时写入同步队列
  - `sync_outbox` 表
  - 包含操作类型和 payload

**产出物**：`apps/desktop/src/lib/repositories/task-repository.ts`

---

### 6.2 内置视图（P0/P1）

- [ ] **VIEW-001** Inbox 视图
  - 查询：`project_id IS NULL AND deleted_at IS NULL`
  - 排序：`sort_order ASC`

- [ ] **VIEW-002** 今天视图
  - 查询：`scheduled_date = today OR due_at <= today`
  - 排序：优先级 > 时间 > 手动排序

- [ ] **VIEW-003** 未来 7 天视图
  - 查询：`scheduled_date BETWEEN today AND today+7`
  - 按日期分组显示

- [ ] **VIEW-004** 已逾期视图
  - 查询：`due_at < now AND status = 'todo'`
  - 排序：逾期天数倒序

- [ ] **VIEW-005** 无日期任务视图
  - 查询：`scheduled_date IS NULL AND scheduled_at IS NULL AND due_at IS NULL`
  - 排序：`created_at DESC`

- [ ] **VIEW-006** 已完成视图
  - 查询：`status = 'done'`
  - 排序：`completed_at DESC`

- [ ] **VIEW-007** 回收站视图
  - 查询：`deleted_at IS NOT NULL`
  - 排序：`deleted_at DESC`
  - 显示剩余保留天数

- [ ] **VIEW-008** 收藏视图（P1）
  - 查询：`is_favorite = 1`（需要添加字段）

- [ ] **VIEW-009** 保存的自定义筛选器（P1）
  - 保存筛选条件到 `settings`
  - 支持命名和编辑

**产出物**：`apps/desktop/src/lib/views/`

---

### 6.3 搜索、筛选与排序（P0/P1）

- [ ] **SEARCH-001** 搜索标题和备注
  - SQLite `LIKE` 或 FTS5
  - 防抖 300ms

- [ ] **SEARCH-002** 按项目、标签、优先级、状态筛选
  - 组合筛选条件
  - 显示当前筛选条件

- [ ] **SEARCH-003** 按计划时间、截止时间、创建时间和手动顺序排序
  - 升序/降序切换
  - 保存排序偏好

- [ ] **SEARCH-004** 支持"仅显示父任务"或"展开全部子任务"
  - 切换显示模式
  - 保存偏好

- [ ] **SEARCH-005** 清楚显示当前生效的筛选条件
  - 筛选条件标签
  - 一键清除

- [ ] **SEARCH-006** 为 10,000 条任务建立性能测试
  - 测试查询性能
  - 优化慢查询

- [ ] **SEARCH-007** 支持组合筛选并保存为智能列表（P1）
  - 筛选条件组合
  - 保存到 `settings`

**产出物**：`apps/desktop/src/lib/search.ts`

---

### 6.4 批量操作（P0/P1）

- [ ] **BATCH-001** 多选任务
  - Ctrl+Click 添加/移除选择
  - Shift+Click 范围选择
  - 全选/取消全选

- [ ] **BATCH-002** 批量完成、恢复、删除
  - 单个事务内执行
  - 写入 `sync_outbox`

- [ ] **BATCH-003** 批量移动项目
  - 选择目标项目
  - 单个事务内执行

- [ ] **BATCH-004** 批量添加或移除标签
  - 选择标签
  - 单个事务内执行

- [ ] **BATCH-005** 批量修改优先级
  - 选择优先级
  - 单个事务内执行

- [ ] **BATCH-006** 批量修改日期和提醒（P1）
  - 选择日期/提醒
  - 单个事务内执行

- [ ] **BATCH-007** 批量操作必须可以立即撤销
  - 保存操作前状态
  - 支持 Ctrl+Z 撤销

**产出物**：`apps/desktop/src/lib/batch.ts`

---

### 6.5 父子任务实现（P0）

- [ ] **PARENT-001** 新建子任务
  - 设置 `parent_id`
  - 检查层级深度（最多 3 级）

- [ ] **PARENT-002** 将普通任务缩进或提升层级
  - 缩进：设置 `parent_id` 为上一个兄弟任务
  - 提升：设置 `parent_id` 为父任务的 `parent_id`
  - 检查循环引用

- [ ] **PARENT-003** 折叠和展开子任务
  - 保存折叠状态到 `settings`
  - UI 切换显示

- [ ] **PARENT-004** 显示父任务完成进度
  - 格式：`3/5`
  - 更新时机：子任务状态变化时

- [ ] **PARENT-005** 在单个数据库事务内执行父子完成联动
  - 检查所有子任务状态
  - 自动完成父任务

- [ ] **PARENT-006** 父任务联动产生的每个变化都进入同步队列
  - 记录每个变化
  - 批量写入 `sync_outbox`

- [ ] **PARENT-007** 验证循环引用、越级移动和并发完成
  - 循环引用检测算法
  - 并发完成处理

**产出物**：`apps/desktop/src/lib/repositories/parent-child.ts`

---

## 七、Repository 层设计

### 7.1 TaskRepository

```typescript
interface TaskRepository {
  // CRUD
  create(task: CreateTaskRequest): Promise<Task>;
  update(id: string, updates: UpdateTaskRequest): Promise<Task>;
  delete(id: string): Promise<void>;  // soft delete
  restore(id: string): Promise<Task>;
  permanentlyDelete(id: string): Promise<void>;
  
  // Query
  findById(id: string): Promise<Task | null>;
  findByView(view: ViewType, filters?: Filters): Promise<Task[]>;
  search(query: string): Promise<Task[]>;
  
  // Batch
  batchUpdate(ids: string[], updates: UpdateTaskRequest): Promise<void>;
  batchDelete(ids: string[]): Promise<void>;
  batchRestore(ids: string[]): Promise<void>;
  
  // Parent-child
  getChildren(parentId: string): Promise<Task[]>;
  getDescendants(parentId: string): Promise<Task[]>;
  moveTask(id: string, newParentId: string | null): Promise<void>;
  
  // Sync
  getPendingSync(): Promise<SyncOperation[]>;
  markSynced(operationIds: string[]): Promise<void>;
}
```

### 7.2 ProjectRepository

```typescript
interface ProjectRepository {
  create(project: CreateProjectRequest): Promise<Project>;
  update(id: string, updates: UpdateProjectRequest): Promise<Project>;
  delete(id: string): Promise<void>;
  findAll(): Promise<Project[]>;
  findById(id: string): Promise<Project | null>;
}
```

### 7.3 TagRepository

```typescript
interface TagRepository {
  create(tag: CreateTagRequest): Promise<Tag>;
  update(id: string, updates: UpdateTagRequest): Promise<Tag>;
  delete(id: string): Promise<void>;
  findAll(): Promise<Tag[]>;
  findByTaskId(taskId: string): Promise<Tag[]>;
  addToTask(taskId: string, tagId: string): Promise<void>;
  removeFromTask(taskId: string, tagId: string): Promise<void>;
}
```

---

## 八、执行顺序

```
阶段 1：数据库基础（第 1 周）
├── DB-001 ~ DB-009：SQLite 初始化和迁移
├── TBL-001 ~ TBL-007：核心表创建
└── INT-001 ~ INT-007：约束和索引

阶段 2：任务 CRUD（第 2 周）
├── CRUD-001 ~ CRUD-011：基础操作
├── Repository 层实现
└── 同步队列写入

阶段 3：视图和搜索（第 3 周）
├── VIEW-001 ~ VIEW-007：内置视图
├── SEARCH-001 ~ SEARCH-006：搜索筛选
└── PARENT-001 ~ PARENT-007：父子任务

阶段 4：客户端外壳（第 4 周）
├── WIN-001 ~ WIN-007：窗口管理
├── TRAY-001 ~ TRAY-006：系统托盘
├── KEY-001 ~ KEY-007：快捷键
└── BATCH-001 ~ BATCH-007：批量操作
```

---

## 九、完成标准

M1 阶段完成时，必须满足：

1. ✅ SQLite 数据库初始化和迁移正常工作
2. ✅ 所有核心数据表已创建
3. ✅ 任务 CRUD 操作正常
4. ✅ 内置视图（Inbox、今天、未来 7 天、已逾期、已完成、回收站）正常显示
5. ✅ 搜索和筛选功能正常
6. ✅ 父子任务联动正常
7. ✅ 批量操作正常
8. ✅ 窗口状态保存和恢复正常
9. ✅ 系统托盘功能正常
10. ✅ 快捷键功能正常
11. ✅ 断网时所有功能正常工作
12. ✅ 每次修改都写入同步队列

---

## 十、测试用例

### 10.1 数据库测试

| 测试 | 预期 |
|---|---|
| 首次启动自动创建数据库 | 数据库文件存在，表结构正确 |
| 迁移失败时提示备份 | 显示错误提示，不丢失数据 |
| 并发写入不冲突 | 多个写入操作正常完成 |

### 10.2 任务 CRUD 测试

| 测试 | 预期 |
|---|---|
| 新建任务 | 任务出现在列表中 |
| 编辑任务标题 | 标题更新，同步队列有记录 |
| 完成任务 | 状态变为 done，completed_at 设置 |
| 删除任务 | 进入回收站，同步队列有记录 |
| 恢复任务 | 从回收站移除，同步队列有记录 |

### 10.3 父子任务测试

| 测试 | 预期 |
|---|---|
| 完成所有子任务 | 父任务自动完成 |
| 删除父任务 | 子任务一起删除 |
| 移动到子孙下面 | 拒绝，显示错误 |

### 10.4 离线测试

| 测试 | 预期 |
|---|---|
| 断网创建任务 | 任务保存成功 |
| 断网编辑任务 | 编辑保存成功 |
| 重启应用 | 任务仍然存在 |
| 恢复网络 | 同步队列自动上传 |

---

## 十一、风险与依赖

| 风险项 | 影响 | 应对措施 |
|---|---|---|
| Tauri SQL 插件限制 | 可能不支持某些 SQLite 特性 | 提前验证，必要时使用原生 SQL |
| 性能问题 | 大量任务时查询慢 | 建立索引，分页加载 |
| 并发冲突 | 多个操作同时修改同一任务 | 使用事务，乐观锁 |

---

## 十二、相关文档

- [M0 阶段计划任务](./M0阶段计划任务.md)
- [产品规则文档](./docs/rules/product-rules.md)
- [父子任务规则](./docs/rules/parent-child-rules.md)
- [共享协议](./packages/contracts/)

---

> 本文档定义了 M1 阶段的完整任务清单，建议严格按顺序执行
