# M2 阶段计划任务：时间、重复任务和提醒

> 文档版本：v1.0
> 编制日期：2026-07-22
> 阶段目标：完成本地提醒和重复任务，关闭主窗口后仍能按时提醒
> 前置依赖：M1 阶段完成

---

## 一、任务总览

| 任务组 | 任务数 | 优先级 | 预计产出 |
|---|---|---|---|
| 时间规则 | 7 | P0 | 时间处理模块 |
| 重复规则 | 14 | P0/P1 | 重复任务引擎 |
| 本地提醒调度器 | 13 | P0/P1 | 提醒服务 |
| 提醒验收 | 7 | P0 | 验收测试 |

**总计**：约 41 个任务项

---

## 二、时间规则

### 2.1 时间字段分离（P0）

- [ ] **TIME-001** 区分计划日期、计划时间、截止时间和提醒时间
  - `scheduledDate`：计划日期（全天任务）
  - `scheduledAt`：计划时间（有具体时间）
  - `dueAt`：截止时间
  - `remindAt`：提醒时间（可多个）

- [ ] **TIME-002** 所有精确时间保存 UTC，同时保留原始时区
  - 存储格式：ISO 8601 UTC
  - 时区字段：IANA 格式（如 `Asia/Shanghai`）
  - 显示时转换为本地时间

- [ ] **TIME-003** 全天任务按本地日期存储
  - 使用独立日期字段 `scheduledDate`
  - 禁止用 UTC 零点模拟
  - 格式：`YYYY-MM-DD`

- [ ] **TIME-004** 系统时区改变后重新计算未来提醒
  - 监听系统时区变化事件
  - 重新计算所有待触发提醒的时间
  - 更新 `timezone` 字段

- [ ] **TIME-005** 系统时间被手动修改后重新扫描提醒
  - 监听系统时间变化事件
  - 重新扫描所有待触发提醒
  - 处理已过期的提醒

- [ ] **TIME-006** 夏令时切换测试通过
  - 测试夏令时开始时的时间计算
  - 测试夏令时结束时的时间计算
  - 确保提醒时间正确

- [ ] **TIME-007** 允许一个任务设置多个提醒
  - 数据库支持多条提醒记录
  - UI 支持添加/删除提醒
  - 每个提醒独立触发

**产出物**：`apps/desktop/src/lib/time.ts`

---

## 三、重复规则

### 3.1 重复类型（P0）

- [ ] **REC-001** 每天
  - 完成后生成下一天的任务
  - 保持相同的时间设置

- [ ] **REC-002** 每个工作日
  - 周一至周五
  - 跳过周末

- [ ] **REC-003** 每周指定星期
  - 支持选择多个星期几
  - 如：每周一、三、五

- [ ] **REC-004** 每月指定日期
  - 支持指定日期（如每月15号）
  - 处理月末日期（如每月31号）

- [ ] **REC-005** 每年指定日期
  - 支持指定月日（如每年1月1日）
  - 处理闰年2月29日

- [ ] **REC-006** 完成后每隔 N 天
  - 从完成日期开始计算
  - 如：完成后每隔3天

### 3.2 重复设置（P0）

- [ ] **REC-007** 设置重复开始日期
  - 默认为任务创建日期
  - 可手动修改

- [ ] **REC-008** 设置重复结束日期
  - 可选：永不结束、指定日期、指定次数
  - 达到结束条件后停止生成新实例

- [ ] **REC-009** 完成本次后生成下一次实例
  - 标记当前实例为完成
  - 自动生成下一个实例
  - 保持相同的时间设置

- [ ] **REC-010** 支持跳过本次
  - 标记当前实例为跳过
  - 不生成下一个实例
  - 继续等待原定的下一次

### 3.3 高级重复（P1）

- [ ] **REC-011** 每月第 N 个星期几
  - 如：每月第二个周一
  - 处理月份天数不同的情况

- [ ] **REC-012** 每月最后一天
  - 自动识别月份天数
  - 处理2月、闰年

- [ ] **REC-013** 只修改本次、此次及以后或整个系列
  - 修改本次：只影响当前实例
  - 此次及以后：影响当前和未来实例
  - 整个系列：影响所有实例

### 3.4 重复父任务（P0）

- [ ] **REC-014** 重复父任务可以重置指定子任务
  - 配置哪些子任务需要重置
  - 重置子任务状态为待办
  - 保持子任务结构

**产出物**：`apps/desktop/src/lib/recurrence.ts`

---

## 四、本地提醒调度器

### 4.1 提醒服务（P0）

- [ ] **REM-001** 建立独立的提醒调度服务
  - 独立于主窗口运行
  - 使用定时器检查提醒
  - 支持动态添加/取消提醒

- [ ] **REM-002** 应用启动时加载未来提醒
  - 从数据库加载所有待触发提醒
  - 按时间排序
  - 注册到调度器

- [ ] **REM-003** 任务修改后取消旧提醒并重新调度
  - 检测任务时间变化
  - 取消旧的提醒调度
  - 创建新的提醒调度

- [ ] **REM-004** 应用从睡眠恢复时扫描错过的提醒
  - 检测系统睡眠/唤醒事件
  - 扫描睡眠期间错过的提醒
  - 弹出错过的提醒通知

- [ ] **REM-005** 恢复网络不应重复弹出已经处理的提醒
  - 记录提醒处理状态
  - 跳过已处理的提醒
  - 只弹出未处理的提醒

- [ ] **REM-006** 主窗口关闭到托盘后提醒继续运行
  - 提醒服务在后台运行
  - 不依赖主窗口状态
  - 托盘图标显示提醒状态

### 4.2 提醒通知（P0）

- [ ] **REM-007** 通知点击后打开对应任务
  - 传递任务ID到主窗口
  - 定位到对应任务
  - 打开任务详情

- [ ] **REM-008** 支持"完成"和"稍后提醒"
  - 完成：标记任务为完成
  - 稍后提醒：延迟提醒时间

- [ ] **REM-009** 稍后提醒支持 10 分钟、1 小时、明天
  - 10 分钟后重新提醒
  - 1 小时后重新提醒
  - 明天同一时间重新提醒

- [ ] **REM-010** 记录提醒已触发、已确认、已稍后和已取消状态
  - `pending`：等待触发
  - `triggered`：已触发
  - `confirmed`：已确认
  - `snoozed`：已稍后
  - `cancelled`：已取消

### 4.3 安静时段（P0/P1）

- [ ] **REM-011** 安静时段内将普通提醒延后到安静时段结束
  - 设置安静时段（如 22:00 - 08:00）
  - 普通提醒延后到安静时段结束
  - 记录原始提醒时间

- [ ] **REM-012** 重要任务允许绕过应用内安静时段（P1）
  - 标记任务为重要
  - 重要任务在安静时段仍能提醒
  - 用户可配置是否启用

- [ ] **REM-013** 研究 Windows 原生计划通知（P1）
  - 调研 Windows 通知 API
  - 降低对托盘常驻的依赖
  - 提高提醒可靠性

**产出物**：`apps/desktop/src/lib/reminder.ts`

---

## 五、提醒验收

### 5.1 基础验收（P0）

- [ ] **ACC-REM-001** 锁屏时能显示通知
  - Windows 锁屏状态下
  - 通知正常弹出
  - 点击可解锁并打开任务

- [ ] **ACC-REM-002** 主窗口关闭但托盘运行时能显示通知
  - 主窗口最小化到托盘
  - 通知正常弹出
  - 点击可打开主窗口

- [ ] **ACC-REM-003** 电脑睡眠越过提醒时间后，唤醒时出现错过提醒
  - 设置一个未来提醒
  - 电脑进入睡眠
  - 唤醒后弹出错过的提醒

### 5.2 任务变更验收（P0）

- [ ] **ACC-REM-004** 修改任务时间后旧通知不会再出现
  - 设置提醒时间
  - 修改任务时间
  - 旧时间不再触发提醒

- [ ] **ACC-REM-005** 删除任务后其通知不会再出现
  - 设置提醒
  - 删除任务
  - 提醒不再触发

- [ ] **ACC-REM-006** 同一提醒在单台设备只显示一次
  - 提醒触发后标记为已处理
  - 不会重复弹出

### 5.3 重复任务验收（P0）

- [ ] **ACC-REM-007** 重复任务完成后下一实例提醒正确
  - 完成重复任务
  - 自动生成下一实例
  - 下一实例的提醒时间正确

**产出物**：`docs/acceptance/m2-reminder-scenarios.md`

---

## 六、执行顺序

```
阶段 1：时间规则（第 1 周）
├── TIME-001 ~ TIME-007：时间字段分离和时区处理
└── 创建时间处理工具函数

阶段 2：重复规则（第 2 周）
├── REC-001 ~ REC-010：基础重复规则
├── REC-014：重复父任务重置子任务
└── 创建重复任务引擎

阶段 3：提醒调度器（第 3 周）
├── REM-001 ~ REM-006：提醒服务核心
├── REM-007 ~ REM-010：提醒通知和交互
├── REM-011：安静时段
└── 创建提醒调度器

阶段 4：验收测试（第 4 周）
├── ACC-REM-001 ~ ACC-REM-007：提醒验收
├── 边界测试（月末、闰年、夏令时）
└── 编写验收文档
```

---

## 七、技术设计

### 7.1 时间处理模块

```typescript
// apps/desktop/src/lib/time.ts

export interface TimeInfo {
  utc: string;           // ISO 8601 UTC
  local: string;         // 本地时间显示
  timezone: string;      // IANA 时区
  isAllDay: boolean;     // 是否全天任务
  date?: string;         // 全天任务日期 YYYY-MM-DD
}

export function parseTime(input: string, timezone?: string): TimeInfo;
export function formatForDisplay(utc: string, timezone: string): string;
export function isOverdue(dueAt: string): boolean;
export function isToday(date: string): boolean;
export function isTomorrow(date: string): boolean;
export function getRelativeTime(date: string): string;
```

### 7.2 重复规则引擎

```typescript
// apps/desktop/src/lib/recurrence.ts

export interface RecurrenceConfig {
  frequency: 'daily' | 'weekdays' | 'weekly' | 'monthly' | 'yearly' | 'custom';
  interval?: number;        // 间隔
  daysOfWeek?: number[];    // 星期几 [0-6]
  dayOfMonth?: number;      // 每月几号
  monthOfYear?: number;     // 每年几月
  startDate: string;        // 开始日期
  endDate?: string;         // 结束日期
  maxOccurrences?: number;  // 最大次数
}

export function getNextOccurrence(config: RecurrenceConfig, currentDate: string): string;
export function generateOccurrences(config: RecurrenceConfig, count: number): string[];
export function isRecurrenceDate(config: RecurrenceConfig, date: string): boolean;
export function shouldResetForRecurrence(taskId: string): boolean;
```

### 7.3 提醒调度器

```typescript
// apps/desktop/src/lib/reminder.ts

export interface ReminderInfo {
  id: string;
  taskId: string;
  remindAt: string;       // ISO 8601 UTC
  status: 'pending' | 'triggered' | 'confirmed' | 'snoozed' | 'cancelled';
  snoozedUntil?: string;
}

export class ReminderScheduler {
  constructor();
  
  // 生命周期
  start(): Promise<void>;
  stop(): void;
  
  // 提醒管理
  addReminder(reminder: ReminderInfo): void;
  removeReminder(id: string): void;
  updateReminder(id: string, updates: Partial<ReminderInfo>): void;
  
  // 触发处理
  onTrigger(callback: (reminder: ReminderInfo) => void): void;
  confirmReminder(id: string): void;
  snoozeReminder(id: string, duration: '10m' | '1h' | 'tomorrow'): void;
  cancelReminder(id: string): void;
  
  // 系统事件
  onSystemResume(): void;
  onTimezoneChange(): void;
}
```

---

## 八、完成标准

M2 阶段完成时，必须满足：

1. ✅ 所有时间字段正确分离
2. ✅ 时区处理正确（本地时间、UTC、夏令时）
3. ✅ 重复规则正确生成下一实例
4. ✅ 重复父任务可以重置子任务
5. ✅ 提醒在主窗口关闭后仍能触发
6. ✅ 提醒在电脑睡眠唤醒后能补发
7. ✅ 安静时段正常工作
8. ✅ 所有验收场景通过

---

## 九、风险与依赖

| 风险项 | 影响 | 应对措施 |
|---|---|---|
| Windows 通知 API 限制 | 提醒可能不显示 | 使用 Tauri 通知插件 |
| 睡眠唤醒检测 | 错过提醒 | 定时扫描机制 |
| 时区变化检测 | 提醒时间错误 | 监听系统事件 |
| 重复规则复杂性 | 边界情况多 | 充分测试 |

---

## 十、测试用例

### 10.1 时间规则测试

| 测试 | 预期 |
|---|---|
| 创建全天任务 | 日期正确，时间为空 |
| 创建有时间任务 | UTC 时间正确，时区保留 |
| 系统时区改变 | 提醒时间重新计算 |
| 夏令时切换 | 提醒时间正确 |

### 10.2 重复规则测试

| 测试 | 预期 |
|---|---|
| 每天重复 | 完成后生成明天任务 |
| 每周一三五重复 | 完成后生成下一个指定日期 |
| 每月31号重复 | 2月生成28/29号任务 |
| 重复结束日期 | 达到后停止生成 |

### 10.3 提醒测试

| 测试 | 预期 |
|---|---|
| 设置提醒 | 提醒按时触发 |
| 修改时间 | 旧提醒取消，新提醒生效 |
| 删除任务 | 提醒取消 |
| 稍后提醒 | 延迟后重新触发 |
| 安静时段 | 普通提醒延后 |

---

## 十一、相关文档

- [M0 阶段计划任务](./M0阶段计划任务.md)
- [M1 阶段计划任务](./M1阶段计划任务.md)
- [产品规则文档](./docs/rules/product-rules.md)
- [共享协议](./packages/contracts/)

---

> 本文档定义了 M2 阶段的完整任务清单，建议严格按顺序执行
