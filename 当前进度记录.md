# EZTODO 当前进度记录

> 记录日期：2026-07-23
> 记录范围：P0-0 ～ P0-2 + 实施计划阶段 0-6
> 工作区状态：存在未提交改动；本记录不代表已提交或发布。

## 总体结论

实施计划阶段 0-6 已完成。自动化门禁全部通过（171 测试 / 22 文件），但**尚未达到最终验收条件**——Tauri 实机验证（SQLite 持久化、原生通知、窗口生命周期）仍为阻塞项。

**需求矩阵状态：** 388 P0 条目中，78 待开发、82 开发中、228 已实现、0 已验证。

## 已完成并验证

### P0-0：工程门禁与可重复验证

- 根目录 `check-all.bat` 可在 Windows 下调用统一门禁。
- `package.json` 已提供依赖、前端、API、矩阵和差异检查的组合命令。
- 最近一次全量验证通过：
  - `npm.cmd run check:all`
  - `cmd.exe /d /c check-all.bat`
  - `git diff --check`
- 验证范围包括：前端 lint、TypeScript、单元测试、构建；API pytest；任务矩阵；Git 差异格式检查。
- 当前 lint 为 **0 error、72 warnings**。警告未阻断门禁，但应在后续质量治理中逐步消除。

### P0-1：数据访问边界与迁移

- 业务模块已通过 SQL Repository 访问数据；在业务层扫描中未发现直接调用 `getDatabase()` 的情况。
- 已覆盖备份、导入、导出、回收站、重复任务、提醒、同步和数据库工具等模块的 Repository 接入。
- 已实现本地存储迁移，包含项目、标签、任务、设置与任务标签关系，并处理旧 ID、父子关系、项目映射和失败时不写迁移标记。
- Browser Repository 已补齐标签关系支持。
- 迁移与 Repository 相关测试已通过。
- 说明：目前仍是“Repository 封装原始 SQL”的兼容性架构；更细粒度的领域仓储设计可作为后续重构项，不影响当前 P0-1 验收。

### P0-2：已具备的基础与本轮收尾

- 已存在任务、项目、标签、设置、批量、重复、提醒、导入导出、备份、回收站等服务/模块基础。
- 主界面已具备任务创建、视图切换、搜索、项目/优先级筛选、选择任务、批量完成/删除、父子任务展示、基础 AI 输入、导入导出和数据管理入口。
- `TaskService` 已补齐并测试以下关键保护：
  - 禁止把任务移动到自身或其任意后代下，避免层级循环。
  - 删除父任务时按子树处理，避免遗留孤儿任务。
  - 还原父任务时递归还原被删除的子任务树。
- 已新增 TaskService 自动化测试，覆盖：后代循环拦截、删除子树顺序、递归还原子树。
- 任务创建、更新、完成、重开、删除和恢复现已统一同步或取消内存提醒；首次完成重复任务会生成下一实例，重复完成不会重复生成。
- 批量完成、删除、恢复改为复用单任务 Use Case，避免绕过父子级联、提醒和重复任务生命周期。
- 主界面已补齐批量移动项目、修改优先级、添加/移除标签；部分失败时仅保留失败项选中并显示结果。
- 新增 TaskService 生命周期与 Task Store 批量操作测试。

#### 本轮新增（P0-2 事务化 + 父子 + 清理 + 测试）

**事务化批量操作与撤销：**
- 新增 `BatchService`（`lib/services/batch-service.ts`），所有批量操作（完成、删除、恢复、改优先级、移动项目、添加/移除标签）通过 `executeBatch()` 统一执行：先快照、再执行、再记录 `UndoableBatch`。
- `BatchResult.undoToken` 非空时表示可撤销；`undoLastBatch()` 按快照恢复状态，且撤销本身不产生新撤销条目。
- `TaskRepository.getSnapshots()` 在 Browser 和 SQLite 两个实现中均已补齐。
- TaskStore 暴露 `undoLastBatch()` 和 `undoableBatch` 状态，UI 批量工具栏显示"↩️ 撤销"按钮。
- 新增 20 个 BatchService 测试。

**父子任务完整交互：**
- `TaskService.moveTask()` 校验：自移、后代移动、最大 3 层深度（`resultingDepth >= 3` 拒绝）。
- `TaskService.createTask()` 在有 `parentId` 时调用 `validateChildDepth()`，第 4 层创建被拒绝。
- `completeTask(id, mode?)` 支持 `'self'`（仅完成父）和 `'withChildren'`（完成所有后代）；无模式且有未完成子任务时返回 `HAS_UNCOMPLETED_CHILDREN` 哨兵。
- `reopenTask(id, reopenChildren?)` 可选同时恢复已完成子任务。
- 子截止晚于父时 `updateTask` 返回 `warnings[]`。
- Store 新增 `indentTask()`（缩进到前一个兄弟下）和 `outdentTask()`（提升到父的父级）。
- UI：任务详情面板增加缩进/提升按钮；完成有子任务的父时弹出三选项对话框；重开父任务时询问是否恢复子任务。
- 新增 13 个层级测试。

**消除 App 壳层临时流程：**
- AI 创建改为"解析 → 确认卡 → 用户编辑/确认 → 写入"；取消不写入数据库。
- 导入导出改用 `exportTasks()`/`downloadExport()` 和 `importTasks()` 服务，移除 App.tsx 内联 FileReader/Blob 逻辑。
- 筛选从客户端 `filteredTasks` 数组改为 Store `setFilters()` → Repository `findByView(view, filters)`。

**集成测试和验收证据：**
- 新增 Store 集成测试 18 个（CRUD、筛选、批量撤销、缩进提升、父完成三选项、父重开恢复）。
- 新增 10,000 条任务性能测试 9 个，全部查询 P95 < 12ms（目标 200ms）。
- 测试总数从 76 提升至 138（15 个测试文件），0 失败。

## 最近自动化验证结果

| 检查项 | 结果 |
| --- | --- |
| 前端测试 | 15 个测试文件，138 个测试通过，0 失败 |
| 前端构建与 TypeScript | 通过（0 error） |
| 前端 lint | 0 error，69 warnings |
| API pytest | 18 个测试通过 |
| P0 任务矩阵 | source P0 388 / matrix unique P0 388 |
| Windows 批处理门禁 | 通过 |
| `git diff --check` | 通过（仅 CRLF 警告） |
| 性能测试（10,000 条） | 全部查询 P95 < 12ms |

## P0-2 尚未完成 / 不可验收项

1. **Tauri SQLite 事务原子性未实机验证**：BatchService 已实现快照+撤销，但 SQLite BEGIN/COMMIT/ROLLBACK 回滚行为仅通过 Browser 仓库测试，未在 Tauri 环境验证。
2. **Tauri 原生通知未验证**：提醒调度已接入任务生命周期，但原生通知显示、稍后提醒、完成任务后取消通知、通知点击导航均未在 Tauri 运行时验证。
3. **Tauri 持久化未验证**：SQLite 数据重启后保留未验证。
4. **回收站端到端验证缺失**：软删除、子树还原、过期清理、永久删除二次确认尚未形成完整可证据化流程。
5. **数据管理流程不完整**：导入、导出、备份需补齐预览、进度/取消、重复数据策略、完整恢复保护和集成测试。
6. **Due date warning UI 未展示**：服务层已返回 `warnings[]`（子截止晚于父），但 UI 仅 console.log，未向用户展示。
7. **父完成/重开对话框为原生 prompt/confirm**：应改为应用内 Modal 组件。
8. **文本编码问题**：旧验收文档存在中文乱码，需 UTF-8 清理。

## 下一步执行顺序

1. ~~将批量操作升级为 SQLite 业务事务~~ ✅ 已完成（BatchService + 快照 + 撤销）
2. ~~将 AI、导入、导出从 App 壳层迁移到已有服务~~ ✅ 已完成（确认卡片 + 服务调用）
3. ~~补足父子任务的移动/缩进/取消缩进 UI~~ ✅ 已完成（缩进/提升 + 三选项 + 重开恢复）
4. ~~为关键交互添加集成测试~~ ✅ 已完成（138 测试 / 15 文件）
5. **在 Tauri 环境执行一次完整验证**（SQLite 持久化、原生通知、托盘、窗口生命周期）
6. **将父完成/重开对话框从原生 prompt/confirm 改为应用内 Modal**
7. **Due date warning 在 UI 中展示**
8. 清理中文乱码与 lint warnings，重新运行全量门禁后再申请 P0-2 最终验收。

## P0-2 Tauri 手工验收清单

- 使用全新用户数据目录启动，创建任务后重启，确认 SQLite 数据仍存在。
- 使用旧版 `eztodo_db` 数据执行迁移，确认项目、标签、任务、父子关系与设置均正确迁入。
- 创建提醒，验证原生通知；测试稍后提醒和完成任务后的提醒取消。
- 测试托盘隐藏/显示、关闭最小化到托盘、全局快捷键、通知点击后定位到对应任务。
- 测试父任务删除与还原；确认子树行为、永久删除确认和过期清理符合预期。
- 测试重复任务完成后是否生成下一实例，编辑/删除后是否更新或取消对应提醒。
- 测试导入、导出与备份恢复的预览、取消、重复数据策略及失败保护。

## 关联证据

- `docs/acceptance/p0-0-evidence.md`
- `docs/acceptance/p0-1-evidence.md`
- `docs/acceptance/p0-2-evidence.md`
- `docs/acceptance/m2-evidence.md`

## 交接说明

- 当前工作区改动均未提交；不要使用重置或覆盖操作清理工作区。
- P0-2 当前状态应标为”自动化部分通过 / 待 Tauri 实机验收”，不能标记为”最终完成”。
- 每次补齐一项后，应先增加对应测试或手工证据，再运行 `npm.cmd run check:all` 与 `cmd.exe /d /c check-all.bat`。
