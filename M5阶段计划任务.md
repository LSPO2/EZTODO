# M5 阶段计划任务：回收站、导入导出和备份

> 文档版本：v1.0
> 编制日期：2026-07-22
> 阶段目标：可以恢复误删和迁移全部数据
> 前置依赖：M4 阶段完成

---

## 一、任务总览

| 任务组 | 任务数 | 优先级 | 预计产出 |
|---|---|---|---|
| 回收站 | 8 | P0 | 回收站功能 |
| 导入 | 12 | P0/P1 | 导入功能 |
| 导出与备份 | 9 | P0/P1 | 导出备份功能 |

**总计**：约 29 个任务项

---

## 二、回收站

### 2.1 删除与恢复（P0）

- [ ] **TRASH-001** 所有删除默认进入回收站
  - 软删除：设置 `deleted_at` 字段
  - 不物理删除数据
  - 保留同步墓碑

- [ ] **TRASH-002** 删除后显示即时撤销
  - Toast 通知显示"已删除"
  - 包含"撤销"按钮
  - 5 秒后自动消失

- [ ] **TRASH-003** 回收站显示原项目、删除时间和剩余保留天数
  - 显示原项目名称
  - 显示删除时间
  - 显示剩余天数（30天）

- [ ] **TRASH-004** 支持单个和批量恢复
  - 单个恢复：点击恢复按钮
  - 批量恢复：多选后批量恢复
  - 恢复后回到原位置

- [ ] **TRASH-005** 恢复父任务时一并恢复其子任务
  - 检测父子关系
  - 级联恢复所有子孙
  - 保持原有层级

- [ ] **TRASH-006** 支持手动永久删除并二次确认
  - 永久删除按钮
  - 二次确认对话框
  - 提示不可恢复

- [ ] **TRASH-007** 30 天后后台物理清理
  - 定时任务检查
  - 清理超过 30 天的任务
  - 记录清理日志

- [ ] **TRASH-008** 清理前确保删除墓碑已同步到服务器
  - 检查同步状态
  - 未同步的不清理
  - 确保数据一致性

**产出物**：`apps/desktop/src/lib/trash.ts`

---

## 三、导入

### 3.1 导入格式（P0/P1）

- [ ] **IMPORT-001** 支持标准 CSV（P0）
  - UTF-8 编码
  - 列名映射
  - 数据验证

- [ ] **IMPORT-002** 支持本软件完整 JSON 备份（P0）
  - 完整数据结构
  - 包含所有关联数据
  - 版本兼容性

- [ ] **IMPORT-003** 支持 Markdown 复选框（P1）
  - 解析 `- [ ]` 和 `- [x]`
  - 提取任务标题
  - 识别完成状态

- [ ] **IMPORT-004** 支持常见 Todo 软件导出格式（P1）
  - Todoist 格式
  - Things 格式
  - 其他常见格式

### 3.2 导入处理（P0）

- [ ] **IMPORT-005** 自动识别 UTF-8、UTF-8 BOM
  - 检测文件编码
  - 处理 BOM 标记
  - 报告不支持的编码

- [ ] **IMPORT-006** 提供列名和字段映射
  - 自动匹配列名
  - 手动映射界面
  - 保存映射配置

- [ ] **IMPORT-007** 导入前预览前若干条数据
  - 显示前 10 条
  - 显示列映射
  - 确认导入

- [ ] **IMPORT-008** 检测重复 ID 和疑似重复任务
  - ID 重复检测
  - 标题相似度检测
  - 提示用户选择

- [ ] **IMPORT-009** 无效日期、层级或提醒进入错误报告
  - 记录错误详情
  - 显示错误报告
  - 允许跳过或修复

- [ ] **IMPORT-010** 导入使用事务或独立批次 ID
  - 事务保证原子性
  - 批次 ID 用于撤销
  - 失败时回滚

- [ ] **IMPORT-011** 支持整批撤销
  - 记录导入批次
  - 撤销整个批次
  - 恢复导入前状态

- [ ] **IMPORT-012** 大文件导入显示进度并允许取消
  - 进度条显示
  - 取消按钮
  - 部分导入处理

**产出物**：`apps/desktop/src/lib/import.ts`

---

## 四、导出与备份

### 4.1 导出功能（P0/P1）

- [ ] **EXPORT-001** 导出完整 JSON（P0）
  - 包含任务、子任务
  - 包含标签、提醒
  - 包含重复规则
  - 格式版本号

- [ ] **EXPORT-002** 导出通用 CSV（P0）
  - 标准 CSV 格式
  - UTF-8 编码
  - 列名说明

- [ ] **EXPORT-003** 导出 Markdown（P1）
  - 复选框格式
  - 按项目分组
  - 保留层级结构

- [ ] **EXPORT-004** 导出文件包含格式版本和创建时间
  - 版本号：1.0
  - 创建时间：ISO 8601
  - 应用版本

### 4.2 备份功能（P0）

- [ ] **BACKUP-005** 提供手动本地备份
  - 一键备份按钮
  - 选择保存位置
  - 显示备份大小

- [ ] **BACKUP-006** 提供一键恢复并显示影响范围
  - 选择备份文件
  - 显示影响范围
  - 确认恢复

- [ ] **BACKUP-007** 恢复前自动备份当前数据库
  - 自动创建备份
  - 备份命名规则
  - 保留最近备份

- [ ] **BACKUP-008** 备份和恢复均校验文件完整性
  - SHA-256 校验
  - 文件大小检查
  - 格式验证

- [ ] **BACKUP-009** 支持定时自动备份和保留数量设置（P1）
  - 配置备份间隔
  - 配置保留数量
  - 自动清理旧备份

**产出物**：`apps/desktop/src/lib/backup.ts`

---

## 五、执行顺序

```
阶段 1：回收站（第 1 周）
├── TRASH-001 ~ TRASH-004：删除和恢复
├── TRASH-005 ~ TRASH-008：级联删除和清理
└── 创建回收站 UI 组件

阶段 2：导入功能（第 2 周）
├── IMPORT-001 ~ IMPORT-004：导入格式
├── IMPORT-005 ~ IMPORT-008：导入处理
├── IMPORT-009 ~ IMPORT-012：错误处理
└── 创建导入 UI 组件

阶段 3：导出与备份（第 3 周）
├── EXPORT-001 ~ EXPORT-004：导出功能
├── BACKUP-005 ~ BACKUP-009：备份功能
└── 创建导出备份 UI 组件

阶段 4：测试和验收（第 4 周）
├── 测试所有功能
├── 编写验收文档
└── 修复问题
```

---

## 六、技术设计

### 6.1 回收站数据结构

```typescript
interface TrashItem {
  id: string;
  entityType: 'task' | 'project' | 'tag';
  entityId: string;
  entityData: any;  // 完整数据快照
  parentId: string | null;
  originalProjectId: string | null;
  deletedAt: string;  // ISO 8601
  expiresAt: string;  // ISO 8601 (deletedAt + 30 days)
  syncedAt: string | null;  // 同步到服务器的时间
}
```

### 6.2 导入格式定义

```typescript
interface ImportFormat {
  type: 'csv' | 'json' | 'markdown';
  encoding: 'utf-8' | 'utf-8-bom';
  columns: ColumnMapping[];
}

interface ColumnMapping {
  source: string;  // 源列名
  target: string;  // 目标字段
  transform?: (value: string) => any;  // 转换函数
}

interface ImportResult {
  success: boolean;
  imported: number;
  skipped: number;
  errors: ImportError[];
  batchId: string;  // 用于撤销
}

interface ImportError {
  row: number;
  field: string;
  message: string;
  value: string;
}
```

### 6.3 备份格式定义

```typescript
interface BackupMetadata {
  version: string;  // "1.0"
  createdAt: string;  // ISO 8601
  appVersion: string;
  checksum: string;  // SHA-256
  itemCounts: {
    tasks: number;
    projects: number;
    tags: number;
    reminders: number;
    recurrenceRules: number;
  };
}

interface BackupFile {
  metadata: BackupMetadata;
  data: {
    tasks: Task[];
    projects: Project[];
    tags: Tag[];
    reminders: Reminder[];
    recurrenceRules: RecurrenceRule[];
  };
}
```

---

## 七、完成标准

M5 阶段完成时，必须满足：

1. ✅ 所有删除进入回收站
2. ✅ 可以恢复删除的任务
3. ✅ 恢复父任务时子任务一起恢复
4. ✅ 30 天后自动清理
5. ✅ 可以导入 CSV 和 JSON
6. ✅ 导入前可以预览
7. ✅ 导入可以撤销
8. ✅ 可以导出 JSON 和 CSV
9. ✅ 可以手动备份和恢复
10. ✅ 备份恢复校验完整性

---

## 八、测试用例

### 回收站测试

| 测试 | 预期 |
|---|---|
| 删除任务 | 进入回收站 |
| 恢复任务 | 回到原位置 |
| 删除父任务 | 子任务一起删除 |
| 恢复父任务 | 子任务一起恢复 |
| 永久删除 | 不可恢复 |
| 30天清理 | 自动删除 |

### 导入测试

| 测试 | 预期 |
|---|---|
| 导入 CSV | 成功导入 |
| 导入 JSON | 成功导入 |
| 导入重复 ID | 检测到重复 |
| 导入无效数据 | 报告错误 |
| 撤销导入 | 恢复原状 |

### 导出测试

| 测试 | 预期 |
|---|---|
| 导出 JSON | 完整数据 |
| 导出 CSV | 正确格式 |
| 备份数据库 | 创建备份 |
| 恢复数据库 | 数据完整 |

---

## 九、风险与依赖

| 风险项 | 影响 | 应对措施 |
|---|---|---|
| 大文件导入性能 | 导入缓慢 | 分批处理，显示进度 |
| 数据格式兼容性 | 导入失败 | 严格验证，错误报告 |
| 备份文件损坏 | 恢复失败 | 校验完整性，自动备份 |
| 回收站数据膨胀 | 存储空间 | 定期清理，压缩存储 |

---

## 十、相关文档

- [M0 阶段计划任务](./M0阶段计划任务.md)
- [M1 阶段计划任务](./M1阶段计划任务.md)
- [M2 阶段计划任务](./M2阶段计划任务.md)
- [M3 阶段计划任务](./M3阶段计划任务.md)
- [M4 阶段计划任务](./M4阶段计划任务.md)

---

> 本文档定义了 M5 阶段的完整任务清单，建议严格按顺序执行
