# 基线记录 — 2026-07-23

## 阶段 0 冻结基线

### 环境
- 操作系统：Windows 11 Home 10.0.22621
- Node.js：>=18.0.0
- Rust toolchain：stable-x86_64-pc-windows-msvc
- Python：3.11.9
- Git 分支：develop（领先 origin/develop 2 个提交）

### 基线命令结果

| 命令 | 结果 |
|------|------|
| `npm run check:all` | ✅ 通过 |
| `npm run build:contracts` | ✅ 通过 |
| `cargo fmt --check` | ✅ 通过（需 rustfmt 组件） |
| `cargo clippy -- -D warnings` | ✅ 通过（需 clippy 组件） |
| `git diff --check` | ✅ 通过（仅 CRLF 警告） |

### 测试统计
- 前端：20 文件，159 项测试通过
- API：18 项测试通过（仅 AI parser）
- ESLint：0 error，67 warning

### 基线分支
- 备份分支：`backup/pre-plan-baseline`
- 可通过 `git checkout backup/pre-plan-baseline` 回退到此状态

### 变更记录
- `.gitignore`：移除 `Cargo.lock`（应用项目应提交 Cargo.lock）
- `package.json`：新增 `check:contracts` 和 `check:rust` 脚本，`check:all` 已包含 `check:contracts`
- 安装了 `rustfmt` 和 `clippy` Rust 组件

### 注意事项
- `check:rust` 未包含在 `check:all` 中（Rust 编译耗时较长，建议单独运行）
- 7 个审计代理已全部完成，发现 388 P0 条目中 188 待开发、82 开发中、58 已实现、0 已验证
- 11 条 DoD 全部未勾选
