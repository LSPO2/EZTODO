# M0 阶段计划任务：需求、规则和原型

> 文档版本：v1.0
> 编制日期：2026-07-22
> 阶段目标：确定产品规则、界面原型和数据模型，建立工程基础

---

## 一、任务总览

| 任务组 | 任务数 | 优先级 | 预计产出 |
|---|---|---|---|
| 产品规则确认 | 12 | P0/P1 | 规则文档 |
| 父子任务规则 | 7 | P0/P1 | 规则文档 |
| 页面与交互原型 | 12 | P0/P1 | 原型设计稿 |
| MVP验收场景 | 7 | P0 | 验收清单 |
| 工程基础-仓库结构 | 9 | P0 | 仓库骨架 |
| 工程基础-客户端工程 | 8 | P0/P1 | 客户端项目 |
| 工程基础-服务端工程 | 10 | P0 | 服务端项目 |
| 工程基础-共享协议 | 6 | P0 | 协议定义 |

---

## 二、产品规则确认

### 2.1 核心规则（P0）

- [ ] **R-001** 确认首版只支持单用户，服务端暂不开放注册
- [ ] **R-002** 确认登录方式：服务器地址 + 用户名 + 密码
- [ ] **R-003** 确认任务层级最多 3 级（任务 → 子任务 → 孙任务）
- [ ] **R-004** 确认默认时区跟随 Windows 系统
- [ ] **R-005** 确认日期、截止时间和提醒时间是不同字段
- [ ] **R-006** 确认无时间任务默认不推送，除非用户设置默认提醒规则
- [ ] **R-007** 确认回收站按时间保留 30 天
- [ ] **R-008** 确认父子任务完成规则（见第三章）
- [ ] **R-009** 确认 AI 高置信度结果自动建立，低置信度结果要求确认
- [ ] **R-010** 确认 AI 自动创建后提供明显的撤销入口

### 2.2 扩展规则（P1）

- [ ] **R-011** 确认是否保存 AI 原始输入和解析结果
- [ ] **R-012** 确认语音原始音频是否立即删除

**产出物**：`docs/rules/product-rules.md`

---

## 三、父子任务规则

### 3.1 完成联动（P0）

- [ ] **PR-001** 子任务全部完成时，自动完成父任务
- [ ] **PR-002** 父任务仍有未完成子任务时，完成父任务必须弹出确认
- [ ] **PR-003** 确认框提供"仅完成父任务"和"同时完成所有子任务"两个选项
- [ ] **PR-004** 重新打开父任务时，允许选择是否恢复子任务

### 3.2 层级约束（P0）

- [ ] **PR-005** 禁止把任务移动到自己的子孙任务下面，防止形成循环
- [ ] **PR-006** 父任务删除时，其子任务一起进入回收站

### 3.3 扩展规则（P1）

- [ ] **PR-007** 子任务截止时间晚于父任务时给出警告，但不强制阻止

**产出物**：`docs/rules/parent-child-rules.md`

---

## 四、页面与交互原型

### 4.1 核心页面（P0）

- [ ] **UI-001** 登录与服务器配置页
- [ ] **UI-002** 主界面：左侧导航、任务列表、右侧详情面板
- [ ] **UI-003** Inbox 页面
- [ ] **UI-004** 今天页面
- [ ] **UI-005** 未来 7 天页面
- [ ] **UI-006** 已逾期页面
- [ ] **UI-007** 已完成页面
- [ ] **UI-008** 回收站页面
- [ ] **UI-009** 顶部快速添加栏
- [ ] **UI-010** AI 解析确认卡片
- [ ] **UI-011** 任务详情编辑面板
- [ ] **UI-012** 重复规则编辑器
- [ ] **UI-013** 多提醒编辑器
- [ ] **UI-014** 同步状态和冲突提示
- [ ] **UI-015** 导入预览与错误报告页面

### 4.2 扩展页面（P1）

- [ ] **UI-016** 全局快捷添加小窗口
- [ ] **UI-017** 语音录入与转写确认页面

**产出物**：Figma/Sketch 设计稿 或 HTML 原型

---

## 五、MVP 验收场景

- [ ] **ACC-001** 输入"明天下午三点交实验报告，提前一天和一小时提醒"，能建立正确任务
- [ ] **ACC-002** 输入"每周一晚上八点复习高数"，能建立重复任务
- [ ] **ACC-003** 输入"月底完成实习报告，分为整理照片、写正文、调整格式"，能建立父子任务
- [ ] **ACC-004** 断网创建任务，重启软件后任务仍存在
- [ ] **ACC-005** 恢复网络后，离线任务自动上传服务器
- [ ] **ACC-006** 主窗口关闭到托盘后，提醒仍能正常弹出
- [ ] **ACC-007** 删除一个包含子任务的项目后，可以从回收站完整恢复

**产出物**：`docs/acceptance/mvp-scenarios.md`

---

## 六、工程基础 - 仓库结构

### 6.1 Git 与目录（P0）

- [ ] **GIT-001** 建立 Git 仓库和主分支保护规则
- [ ] **GIT-002** 建立目录结构：
  ```
  EZTODO/
  ├── apps/
  │   └── desktop/          # Tauri 客户端
  ├── services/
  │   └── api/              # FastAPI 服务端
  ├── packages/
  │   └── contracts/        # 共享协议/类型定义
  ├── infra/
  │   └── docker/           # Docker 配置
  └── docs/                 # 文档
  ```
- [ ] **GIT-003** 建立 `.env.example`，不得提交真实密钥
- [ ] **GIT-004** 添加统一的版本号策略（如 `0.1.0-m0`）
- [ ] **GIT-005** 添加开发、测试、生产三套配置

**产出物**：仓库目录结构和配置文件

---

## 七、工程基础 - 客户端工程

### 7.1 项目初始化（P0）

- [ ] **CLI-001** 初始化 Tauri 2 + React + TypeScript + Vite
- [ ] **CLI-002** 配置 ESLint、Prettier 和严格 TypeScript（`strict: true`）
- [ ] **CLI-003** 配置路径别名（`@/` 映射到 `src/`）
- [ ] **CLI-004** 配置单元测试（Vitest）和组件测试（React Testing Library）
- [ ] **CLI-005** 配置开发环境错误边界
- [ ] **CLI-006** 配置结构化日志和日志轮换
- [ ] **CLI-007** 禁止日志记录密码、Token、任务正文和完整 AI 输入

### 7.2 扩展配置（P1）

- [ ] **CLI-008** 配置端到端测试环境（Playwright）

**产出物**：`apps/desktop/` 项目骨架

---

## 八、工程基础 - 服务端工程

### 8.1 项目初始化（P0）

- [ ] **SRV-001** 初始化 FastAPI 项目
- [ ] **SRV-002** 按 `api/domain/services/repositories/models` 分层
- [ ] **SRV-003** 启用 Pydantic 严格校验
- [ ] **SRV-004** 生成 OpenAPI 文档（自动生成）
- [ ] **SRV-005** 配置 SQLAlchemy 2 和 Alembic
- [ ] **SRV-006** 配置 PostgreSQL 测试数据库
- [ ] **SRV-007** 增加 `/health`、`/ready` 和 `/version` 接口
- [ ] **SRV-008** 编写 Dockerfile 和 Docker Compose
- [ ] **SRV-009** 配置 Caddy HTTPS 反向代理
- [ ] **SRV-010** 配置数据库定时备份和恢复脚本

**产出物**：`services/api/` 项目骨架

---

## 九、工程基础 - 共享协议

### 9.1 协议定义（P0）

- [ ] **PRT-001** 定义任务、提醒、重复规则、同步操作的 JSON Schema
- [ ] **PRT-002** 客户端和服务端从同一份 Schema 生成或校验类型
- [ ] **PRT-003** 给 API 增加显式版本，例如 `/api/v1`
- [ ] **PRT-004** 规定所有 ID 使用 UUIDv7 或等价的客户端可生成 UUID
- [ ] **PRT-005** 规定时间戳使用 ISO 8601，并在传输时使用 UTC
- [ ] **PRT-006** 全天任务使用独立日期字段，禁止用 UTC 零点模拟

**产出物**：`packages/contracts/` 协议包

---

## 十、执行顺序

```
阶段 1：产品规则（第 1 周）
├── 完成产品规则确认（R-001 ~ R-012）
├── 完成父子任务规则（PR-001 ~ PR-007）
└── 输出规则文档

阶段 2：页面原型（第 2 周）
├── 完成所有页面原型设计（UI-001 ~ UI-017）
└── 输出设计稿

阶段 3：工程基础（第 3-4 周）
├── 建立仓库结构（GIT-001 ~ GIT-005）
├── 初始化客户端工程（CLI-001 ~ CLI-008）
├── 初始化服务端工程（SRV-001 ~ SRV-010）
└── 定义共享协议（PRT-001 ~ PRT-006）

阶段 4：验收准备（第 4 周）
├── 编写 MVP 验收场景（ACC-001 ~ ACC-007）
└── 确认所有 M0 任务完成
```

---

## 十一、完成标准

M0 阶段完成时，必须满足：

1. ✅ 所有 P0 产品规则已确认并文档化
2. ✅ 父子任务规则已确认并文档化
3. ✅ 所有 P0 页面原型已完成并评审通过
4. ✅ MVP 验收场景已编写并确认
5. ✅ 仓库目录结构已建立
6. ✅ 客户端项目可正常启动（`npm run dev`）
7. ✅ 服务端项目可正常启动（`uvicorn`）
8. ✅ Docker Compose 可一键启动服务端
9. ✅ 共享协议 JSON Schema 已定义
10. ✅ 所有代码通过 lint 检查

---

## 十二、风险与依赖

| 风险项 | 影响 | 应对措施 |
|---|---|---|
| Tauri 2 插件兼容性 | 阻塞客户端开发 | 提前验证 SQL、通知、托盘插件 |
| PostgreSQL 环境搭建 | 阻塞服务端开发 | 使用 Docker 简化部署 |
| 原型设计耗时 | 阻塞 UI 开发 | 优先完成核心页面，迭代补充 |
| UUIDv7 兼容性 | 影响同步协议 | 验证 TypeScript/Python 库支持 |

---

## 十三、相关文档

- [PC端AI_Todo开发任务清单.md](./PC端AI_Todo开发任务清单.md)
- [docs/rules/](./docs/rules/) - 规则文档目录
- [docs/acceptance/](./docs/acceptance/) - 验收文档目录
